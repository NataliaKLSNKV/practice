#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main()
{
setlocale(LC_ALL, "Russian");
ifstream file("file.txt");
if (file.is_open()) {
string a = " ";
while (!file.eof()) {
file >> a;
cout << a << endl;
}
file.close();
}
else {
cout << "Файл не открывается!";
}
return 0;
}
